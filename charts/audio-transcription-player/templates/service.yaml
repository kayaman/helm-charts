apiVersion: v1
kind: Service
metadata:
  name: {{ include "audio-transcription-player.fullname" . }}
  labels:
    {{- include "audio-transcription-player.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "audio-transcription-player.selectorLabels" . | nindent 4 }}
